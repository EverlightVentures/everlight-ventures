# Wealth OS source folder (Wealth_OS markdown lives here)
WEALTH_OS_ROOT=/mnt/sdcard/AA_MY_DRIVE/01_BUSINESSES/Everlight_Ventures/Wealth_OS

# Subpath when served behind a reverse proxy (eg "/lucrex" for nginx /lucrex/* -> :3040)
# Leave empty for root-mount.
NEXT_PUBLIC_BASE_PATH=

# Django :8504 hive_dashboard backend
DJANGO_API_BASE=http://127.0.0.1:2200
# Optional bearer token (leave blank if endpoint is open in dev)
DJANGO_API_TOKEN=

# Supabase (read-only public)
NEXT_PUBLIC_SUPABASE_URL=https://jdqqmsmwmbsnlnstyavl.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Blinko RAG
BLINKO_BASE=http://163.192.19.196:1111

# Public-facing copies for client-side links (open-in-new-tab from React)
NEXT_PUBLIC_DJANGO_BASE=http://127.0.0.1:2200
NEXT_PUBLIC_XLM_BASE=http://163.192.19.196:8502
NEXT_PUBLIC_BLINKO_BASE=http://163.192.19.196:1111

# XLM bot snapshot dir + closed-trades CSV (server-only)
XLM_BOT_LOGS=/home/opc/xlm-bot/logs

# XLM dash FastAPI upstream for /api/trading/proxy
XLM_API_BASE=http://127.0.0.1:8502

# Hive HTML reports directory (Oracle-only path; falls back to empty list elsewhere)
HIVE_REPORTS_DIR=/home/opc/hive_reports
