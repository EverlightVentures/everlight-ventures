# Intel Center sources -- Strategy Assistant

**Total assigned:** 91 resources across 1 categories.

## How to use this manifest

When a user query lands in your domain, READ this manifest FIRST and prefer these sources over guessing. Three modes:

1. **Search:** `intel search <query>` -- full-text across 745 resources
2. **Pull live:** `intel pull <domain>` -- fetch RSS/HTML, cache it, get latest items
3. **Investigate:** `intel investigate <target>` -- multi-source OSINT (port 8677)

Each resource below shows its **use_case** (how YOU specifically use it) and **setup_steps** (how to actually invoke it).

## Education & Training  (91)

### [abandonedspaces.com](https://abandonedspaces.com)
_abandoned buildings and forgotten global places_

**Use case:** Strategy Assistant taps abandonedspaces.com for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://abandonedspaces.com.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [animagraffs.com](https://animagraffs.com)
_Complex machines and systems explained_

**Use case:** Strategy Assistant taps animagraffs.com for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://animagraffs.com.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [archaecologydataservice.ac.uk](https://archaecologydataservice.ac.uk)
_data from thousands of global excavations_

**Use case:** Strategy Assistant taps archaecologydataservice.ac.uk for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://archaecologydataservice.ac.uk.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [batteryuniversity.com](https://batteryuniversity.com)
_Deep dive into battery chemistry and systems_

**Use case:** Strategy Assistant taps batteryuniversity.com for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://batteryuniversity.com.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [boot.dev](https://boot.dev)
_Backend learned through browser-based real projects_

**Use case:** Strategy Assistant taps boot.dev for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://boot.dev.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [bosch-mobility.com](https://bosch-mobility.com)
_See how modern car systems operate_

**Use case:** Strategy Assistant taps bosch-mobility.com for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://bosch-mobility.com.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [bradshawfoundation.com](https://bradshawfoundation.com)
_rock art and cave painting archive_

**Use case:** Strategy Assistant taps bradshawfoundation.com for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://bradshawfoundation.com.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [cdli.earth](https://cdli.earth)
_cuneiform tablet digital library_

**Use case:** Strategy Assistant taps cdli.earth for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://cdli.earth.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [chemicalprocessing.com](https://chemicalprocessing.com)
_Explore chemical systems operations_

**Use case:** Strategy Assistant taps chemicalprocessing.com for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://chemicalprocessing.com.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [chipestimate.com](https://chipestimate.com)
_Chip design systems and silicon architecture_

**Use case:** Strategy Assistant taps chipestimate.com for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://chipestimate.com.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [coddy.tech](https://coddy.tech)
_- daily coding challenges across multiple languages_

**Use case:** Strategy Assistant taps coddy.tech for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://coddy.tech.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [codecrafters.io](https://codecrafters.io)
_Rebuild Redis ,,Git ,SQLite or complex softwares from scratch_

**Use case:** Strategy Assistant taps codecrafters.io for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://codecrafters.io.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [coinarchives.com](https://coinarchives.com)
_Millions of ancient coin records with auction prices_

**Use case:** Strategy Assistant taps coinarchives.com for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://coinarchives.com.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [cpu.land](https://cpu.land)
_Watch how a CPU runs instructions_

**Use case:** Strategy Assistant taps cpu.land for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://cpu.land.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [cryptopals.com](https://cryptopals.com)
_Learn cryptography by breaking real ciphers_

**Use case:** Strategy Assistant taps cryptopals.com for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://cryptopals.com.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [cssgridgarden.com](https://cssgridgarden.com)
_Learn CSS Grid by growing a virtual garden_

**Use case:** Strategy Assistant taps cssgridgarden.com for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://cssgridgarden.com.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [datacenterknowledge.com](https://datacenterknowledge.com)
_Data center infrastructure explained_

**Use case:** Strategy Assistant taps datacenterknowledge.com for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://datacenterknowledge.com.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [devchallenges.io](https://devchallenges.io)
_Full-stack project challenges with real-world design briefs_

**Use case:** Strategy Assistant taps devchallenges.io for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://devchallenges.io.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [digi.vatlib.it](https://digi.vatlib.it)
_Vatican’s digitized ancient manuscripts collection_

**Use case:** Strategy Assistant taps digi.vatlib.it for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://digi.vatlib.it.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [dnsviz.net](https://dnsviz.net)
_Analyze DNS trust chains and resolution paths_

**Use case:** Strategy Assistant taps dnsviz.net for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://dnsviz.net.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [etymonline.com](https://etymonline.com)
_Full origin history of every English word_

**Use case:** Strategy Assistant taps etymonline.com for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://etymonline.com.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [exercism.org](https://exercism.org)
_Code exercises with real mentor feedback in 60 languages_

**Use case:** Strategy Assistant taps exercism.org for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://exercism.org.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [explainshell.com](https://explainshell.com)
_Pastes any command and explains every part instantly_

**Use case:** Strategy Assistant taps explainshell.com for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://explainshell.com.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [falstad.com](https://falstad.com)
_understand circuits and signals flow_

**Use case:** Strategy Assistant taps falstad.com for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://falstad.com.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [findagrave.com](https://findagrave.com)
_Burial records for 200 million people worldwide_

**Use case:** Strategy Assistant taps findagrave.com for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://findagrave.com.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [fireship.io](https://fireship.io)
_modern tech explainers and short courses_

**Use case:** Strategy Assistant taps fireship.io for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://fireship.io.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [forgottenbooks.com](https://forgottenbooks.com)
_rare history books collection_

**Use case:** Strategy Assistant taps forgottenbooks.com for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://forgottenbooks.com.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [freesound.org](https://freesound.org)
_600,000 real-world field recordings searchable_

**Use case:** Strategy Assistant taps freesound.org for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://freesound.org.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [frontendmentor.io](https://frontendmentor.io)
_-frontend challenges with professional Figma designs_

**Use case:** Strategy Assistant taps frontendmentor.io for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://frontendmentor.io.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [gazebosim.org](https://gazebosim.org)
_Simulates robots sensors and physics interactions_

**Use case:** Strategy Assistant taps gazebosim.org for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://gazebosim.org.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [gedbas.genealogy.net](https://gedbas.genealogy.net)
_Millions of community-contributed genealogy records_

**Use case:** Strategy Assistant taps gedbas.genealogy.net for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://gedbas.genealogy.net.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [geonames.org](https://geonames.org)
_Ancient place name etymology lookup_

**Use case:** Strategy Assistant taps geonames.org for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://geonames.org.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [histography.io](https://histography.io)
_timeline of all Wikipedia history events_

**Use case:** Strategy Assistant taps histography.io for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://histography.io.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [howacarworks.com](https://howacarworks.com)
_Simple visuals of car parts working together_

**Use case:** Strategy Assistant taps howacarworks.com for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://howacarworks.com.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [hyperskill.org](https://hyperskill.org)
_Project-based courses in Python, Java,, Go and more_

**Use case:** Strategy Assistant taps hyperskill.org for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://hyperskill.org.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [idigbio.org](https://idigbio.org)
_200 million natural history museum specimens searchable_

**Use case:** Strategy Assistant taps idigbio.org for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://idigbio.org.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [impact.uwo.ca](https://impact.uwo.ca)
_Interactive global map of every Earth impact site_

**Use case:** Strategy Assistant taps impact.uwo.ca for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://impact.uwo.ca.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [internetexchangemap.com](https://internetexchangemap.com)
_Learn how internet traffic gets routed_

**Use case:** Strategy Assistant taps internetexchangemap.com for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://internetexchangemap.com.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [javascript.info](https://javascript.info)
_The most complete modern JavaScript guide_

**Use case:** Strategy Assistant taps javascript.info for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://javascript.info.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [learn.sparkfun.com](https://learn.sparkfun.com)
_electronics parts and how they function_

**Use case:** Strategy Assistant taps learn.sparkfun.com for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://learn.sparkfun.com.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [learnabout-electronics.org](https://learnabout-electronics.org)
_Understand electronic systems_

**Use case:** Strategy Assistant taps learnabout-electronics.org for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://learnabout-electronics.org.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [learnpython.org](https://learnpython.org)
_Fully interactive Python tutorials directly in browser_

**Use case:** Strategy Assistant taps learnpython.org for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://learnpython.org.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [learnshell.org](https://learnshell.org)
_Interactive Shell and Bash scripting lessons in browser_

**Use case:** Strategy Assistant taps learnshell.org for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://learnshell.org.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [learnxinyminutes.com](https://learnxinyminutes.com)
_Learn any programming language syntax_

**Use case:** Strategy Assistant taps learnxinyminutes.com for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://learnxinyminutes.com.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [lightingdesignlab.com](https://lightingdesignlab.com)
_understand Lighting systems designs_

**Use case:** Strategy Assistant taps lightingdesignlab.com for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://lightingdesignlab.com.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [linuxjourney.com](https://linuxjourney.com)
_guided course through all Linux concepts_

**Use case:** Strategy Assistant taps linuxjourney.com for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://linuxjourney.com.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [lithiccastinglab.com](https://lithiccastinglab.com)
_prehistoric stone tool identification resource_

**Use case:** Strategy Assistant taps lithiccastinglab.com for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://lithiccastinglab.com.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [missing.csail.mit.edu](https://missing.csail.mit.edu)
_MIT course on tools developers never formally learn_

**Use case:** Strategy Assistant taps missing.csail.mit.edu for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://missing.csail.mit.edu.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [mitmproxy.org](https://mitmproxy.org)
_inspect application network transactions_

**Use case:** Strategy Assistant taps mitmproxy.org for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://mitmproxy.org.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [mnamon.sns.it](https://mnamon.sns.it)
_Every ancient writing system ever discovered explained_

**Use case:** Strategy Assistant taps mnamon.sns.it for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://mnamon.sns.it.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [morphosource.org](https://morphosource.org)
_Free 3D scans of rare museum fossils worldwide_

**Use case:** Strategy Assistant taps morphosource.org for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://morphosource.org.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [nandgame.com](https://nandgame.com)
_Build a computer from basic components_

**Use case:** Strategy Assistant taps nandgame.com for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://nandgame.com.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [navweaps.com](https://navweaps.com)
_Naval weapon systems and firing mechanisms detailed_

**Use case:** Strategy Assistant taps navweaps.com for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://navweaps.com.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [nuclear-power.com](https://nuclear-power.com)
_explore Reactor systems and nuclear plant operations_

**Use case:** Strategy Assistant taps nuclear-power.com for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://nuclear-power.com.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [numismatics.org](https://numismatics.org)
_Ancient world coin collection and research_

**Use case:** Strategy Assistant taps numismatics.org for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://numismatics.org.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [opencontext.org](https://opencontext.org)
_Free archaeological excavation data worldwide_

**Use case:** Strategy Assistant taps opencontext.org for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://opencontext.org.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [opendss.epri.com](https://opendss.epri.com)
_Explore power distribution and voltage control_

**Use case:** Strategy Assistant taps opendss.epri.com for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://opendss.epri.com.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [orbis.stanford.edu](https://orbis.stanford.edu)
_Roman road network rebuilt with ancient travel times_

**Use case:** Strategy Assistant taps orbis.stanford.edu for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://orbis.stanford.edu.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [overthewire.org](https://overthewire.org)
_Linux and security wargames from beginner level_

**Use case:** Strategy Assistant taps overthewire.org for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://overthewire.org.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [pelagios.org](https://pelagios.org)
_Linking ancient world data across platforms_

**Use case:** Strategy Assistant taps pelagios.org for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://pelagios.org.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [pleiades.stoa.org](https://pleiades.stoa.org)
_GPS coordinates of every known ancient settlement_

**Use case:** Strategy Assistant taps pleiades.stoa.org for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://pleiades.stoa.org.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [practicalmachinist.com](https://practicalmachinist.com)
_understand metalworking systems_

**Use case:** Strategy Assistant taps practicalmachinist.com for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://practicalmachinist.com.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [projecteuler.net](https://projecteuler.net)
_programming problems for deep computational thinking_

**Use case:** Strategy Assistant taps projecteuler.net for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://projecteuler.net.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [realpars.com](https://realpars.com)
_lessons on industrial automation and control systems_

**Use case:** Strategy Assistant taps realpars.com for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://realpars.com.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [refactoring.guru](https://refactoring.guru)
_Design patterns and code smells explained_

**Use case:** Strategy Assistant taps refactoring.guru for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://refactoring.guru.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [regexone.com](https://regexone.com)
_Master regular expressions through guided practices_

**Use case:** Strategy Assistant taps regexone.com for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://regexone.com.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [rfglobalnet.com](https://rfglobalnet.com)
_Radio frequency systems and wireless signal behavior_

**Use case:** Strategy Assistant taps rfglobalnet.com for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://rfglobalnet.com.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [roadmap.sh](https://roadmap.sh)
_learning roadmaps for every developer role_

**Use case:** Strategy Assistant taps roadmap.sh for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://roadmap.sh.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [roboticsbiz.com](https://roboticsbiz.com)
_Learn how robots work in industries_

**Use case:** Strategy Assistant taps roboticsbiz.com for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://roboticsbiz.com.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [scrimba.com](https://scrimba.com)
_Edit live code inside tutorial videos mid-playback_

**Use case:** Strategy Assistant taps scrimba.com for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://scrimba.com.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [semiengineering.com](https://semiengineering.com)
_Semiconductor fabrication systems explained_

**Use case:** Strategy Assistant taps semiengineering.com for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://semiengineering.com.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [sqlbolt.com](https://sqlbolt.com)
_Learn SQL with instant in-browser interactive exercises_

**Use case:** Strategy Assistant taps sqlbolt.com for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://sqlbolt.com.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [sqlzoo.net](https://sqlzoo.net)
_Interactive SQL exercises across multiple database systems_

**Use case:** Strategy Assistant taps sqlzoo.net for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://sqlzoo.net.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [submarinecablemap.com](https://submarinecablemap.com)
_Every undersea internet cable crossing the oceans_

**Use case:** Strategy Assistant taps submarinecablemap.com for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://submarinecablemap.com.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [takeuforward.org](https://takeuforward.org)
_Structured DSA sheets.. trusted_

**Use case:** Strategy Assistant taps takeuforward.org for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://takeuforward.org.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [talkbank.org](https://talkbank.org)
_Largest database of human spoken language_

**Use case:** Strategy Assistant taps talkbank.org for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://talkbank.org.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [tanks-encyclopedia.com](https://tanks-encyclopedia.com)
_breakdowns of armored vehicle systems_

**Use case:** Strategy Assistant taps tanks-encyclopedia.com for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://tanks-encyclopedia.com.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [teachyourselfcs.com](https://teachyourselfcs.com)
_world-class CS curriculum using free resources_

**Use case:** Strategy Assistant taps teachyourselfcs.com for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://teachyourselfcs.com.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [the-saleroom.com](https://the-saleroom.com)
_Historical auction records for rare antiques worldwide_

**Use case:** Strategy Assistant taps the-saleroom.com for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://the-saleroom.com.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [theoi.com](https://theoi.com)
_Ancient Greek mythology sourced from original texts_

**Use case:** Strategy Assistant taps theoi.com for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://theoi.com.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [topostext.org](https://topostext.org)
_Ancient literary texts linked to places_

**Use case:** Strategy Assistant taps topostext.org for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://topostext.org.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [treasurenet.com](https://treasurenet.com)
_World's largest lost mines and treasure database_

**Use case:** Strategy Assistant taps treasurenet.com for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://treasurenet.com.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [tropicos.org](https://tropicos.org)
_1.4 million plant names with 4 million specimens_

**Use case:** Strategy Assistant taps tropicos.org for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://tropicos.org.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [trowelblazers.com](https://trowelblazers.com)
_Database of archaeologists and discoveries_

**Use case:** Strategy Assistant taps trowelblazers.com for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://trowelblazers.com.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [vici.org](https://vici.org)
_map of ancient world sites_

**Use case:** Strategy Assistant taps vici.org for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://vici.org.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [visualgo.net](https://visualgo.net)
_Algorithms and data structures animated_

**Use case:** Strategy Assistant taps visualgo.net for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://visualgo.net.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [wikiart.org](https://wikiart.org)
_250,000 artworks with full provenance history_

**Use case:** Strategy Assistant taps wikiart.org for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://wikiart.org.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [wikimapia.org](https://wikimapia.org)
_Millions of crowdsourced hidden locations globally mapped_

**Use case:** Strategy Assistant taps wikimapia.org for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://wikimapia.org.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [worldhist.org](https://worldhist.org)
_Interactive political map,,,, ancient to modern_

**Use case:** Strategy Assistant taps worldhist.org for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://worldhist.org.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [worldhistory.org](https://worldhistory.org)
_global ancient civilizations encyclopedia_

**Use case:** Strategy Assistant taps worldhistory.org for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://worldhistory.org.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.

### [zooniverse.org](https://zooniverse.org)
_Hundreds of live science research projects_

**Use case:** Strategy Assistant taps zooniverse.org for learning, research, or reference material. Drop key concepts into Blinko (RAG) so the rest of the Hive can recall them.

**Setup:**
  1. Open https://zooniverse.org.
  2. Note key takeaways into `Notes/inbox.org` or push to Blinko via `agentmemory MCP`.
  3. Build a study-track in `05_PERSONAL/` if multi-week.
