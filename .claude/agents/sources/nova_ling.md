# Intel Center sources -- Nova Ling

**Total assigned:** 7 resources across 1 categories.

## How to use this manifest

When a user query lands in your domain, READ this manifest FIRST and prefer these sources over guessing. Three modes:

1. **Search:** `intel search <query>` -- full-text across 745 resources
2. **Pull live:** `intel pull <domain>` -- fetch RSS/HTML, cache it, get latest items
3. **Investigate:** `intel investigate <target>` -- multi-source OSINT (port 8677)

Each resource below shows its **use_case** (how YOU specifically use it) and **setup_steps** (how to actually invoke it).

## AI & Automation  (7)

### [agents-apis-697](https://agents-apis-697) *(curated)*
_AI agents, autonomous workflows, assistants_

**Use case:** Nova Ling reaches for agents-apis-697 when building or extending Hive automation: agents, workflows, prompts, models, vector stores. Slot into existing 03_AUTOMATION_CORE/ scripts.

**Setup:**
  1. Open https://agents-apis-697 for docs.
  2. If it's a model/API, capture endpoint + auth into `06_DEVELOPMENT/everlight_os/secrets/`.
  3. Add a wrapper in `content_tools/` so all agents share one entry point.

### [automation-apis-4825](https://automation-apis-4825) *(curated)*
_Workflow automation, bots, integrations_

**Use case:** Nova Ling reaches for automation-apis-4825 when building or extending Hive automation: agents, workflows, prompts, models, vector stores. Slot into existing 03_AUTOMATION_CORE/ scripts.

**Setup:**
  1. Open https://automation-apis-4825 for docs.
  2. If it's a model/API, capture endpoint + auth into `06_DEVELOPMENT/everlight_os/secrets/`.
  3. Add a wrapper in `content_tools/` so all agents share one entry point.

### [lexica.art](https://lexica.art) *(curated)*
_Search engine for AI-generated image prompts._

**Use case:** Nova Ling reaches for lexica.art when building or extending Hive automation: agents, workflows, prompts, models, vector stores. Slot into existing 03_AUTOMATION_CORE/ scripts.

**Setup:**
  1. Open https://lexica.art for docs.
  2. If it's a model/API, capture endpoint + auth into `06_DEVELOPMENT/everlight_os/secrets/`.
  3. Add a wrapper in `content_tools/` so all agents share one entry point.

### [napkin.ai](https://napkin.ai) *(curated)*
_Turns any text into diagrams and visuals_

**Use case:** Nova Ling reaches for napkin.ai when building or extending Hive automation: agents, workflows, prompts, models, vector stores. Slot into existing 03_AUTOMATION_CORE/ scripts.

**Setup:**
  1. Open https://napkin.ai for docs.
  2. If it's a model/API, capture endpoint + auth into `06_DEVELOPMENT/everlight_os/secrets/`.
  3. Add a wrapper in `content_tools/` so all agents share one entry point.

### [patterned.ai](https://patterned.ai) *(curated)*
_Creates patterns for graphic design._

**Use case:** Nova Ling reaches for patterned.ai when building or extending Hive automation: agents, workflows, prompts, models, vector stores. Slot into existing 03_AUTOMATION_CORE/ scripts.

**Setup:**
  1. Open https://patterned.ai for docs.
  2. If it's a model/API, capture endpoint + auth into `06_DEVELOPMENT/everlight_os/secrets/`.
  3. Add a wrapper in `content_tools/` so all agents share one entry point.

### [rationale.jina.ai](https://rationale.jina.ai) *(curated)*
_Breaks down any decision with structured logic_

**Use case:** Nova Ling reaches for rationale.jina.ai when building or extending Hive automation: agents, workflows, prompts, models, vector stores. Slot into existing 03_AUTOMATION_CORE/ scripts.

**Setup:**
  1. Open https://rationale.jina.ai for docs.
  2. If it's a model/API, capture endpoint + auth into `06_DEVELOPMENT/everlight_os/secrets/`.
  3. Add a wrapper in `content_tools/` so all agents share one entry point.

### [social-media-apis-3268](https://social-media-apis-3268) *(curated)*
_Social media automation/data_

**Use case:** Nova Ling reaches for social-media-apis-3268 when building or extending Hive automation: agents, workflows, prompts, models, vector stores. Slot into existing 03_AUTOMATION_CORE/ scripts.

**Setup:**
  1. Open https://social-media-apis-3268 for docs.
  2. If it's a model/API, capture endpoint + auth into `06_DEVELOPMENT/everlight_os/secrets/`.
  3. Add a wrapper in `content_tools/` so all agents share one entry point.
