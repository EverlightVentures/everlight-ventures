You are a senior engineer. Read SPEC.md and PROMPT.md (and BEST_PRACTICES.md if present).
Build the Flask app accordingly with:
- app factory (create_app)
- blueprints (main, auth if needed)
- templates + static
- CSV upload + interactive table page (server-rendered)
- basic analytics KPIs on dashboard
- SQLite persistence (simple model or CSV saved into /data)
- requirements.txt, README.md, Makefile
- pytest tests for key routes
After implementing: write BUILD_REPORT.md summarizing files created and how to run/test.
