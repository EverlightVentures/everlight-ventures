===== FILE: templates/base.html =====
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}{{ business_name }}{% endblock %}</title>

    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">

    <!-- Lucide Icons -->
    <script src="https://unpkg.com/lucide@latest"></script>

    <style>
        :root {
            --bg-dark: #0f1419;
            --bg-card: #1a1f26;
            --bg-card-hover: #242b35;
            --bg-sidebar: #151a21;
            --border: #2d3640;
            --text-primary: #e7e9ea;
            --text-secondary: #8b98a5;
            --text-muted: #5c6b7a;
            --accent-green: #00ba7c;
            --accent-green-hover: #00d68f;
            --accent-blue: #1d9bf0;
            --accent-orange: #ff7a00;
            --accent-red: #f4212e;
            --accent-yellow: #ffd400;
            --accent-purple: #7856ff;
            --shadow: 0 4px 12px rgba(0, 0, 0, 0.4);
            --radius: 12px;
            --radius-sm: 8px;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'DM Sans', -apple-system, sans-serif;
            background: var(--bg-dark);
            color: var(--text-primary);
            min-height: 100vh;
            display: flex;
        }

        /* Sidebar */
        .sidebar {
            width: 260px;
            background: var(--bg-sidebar);
            border-right: 1px solid var(--border);
            height: 100vh;
            position: fixed;
            display: flex;
            flex-direction: column;
            z-index: 100;
        }

        .sidebar-header {
            padding: 24px 20px;
            border-bottom: 1px solid var(--border);
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .logo-icon {
            width: 44px;
            height: 44px;
            background: linear-gradient(135deg, var(--accent-green), #00856b);
            border-radius: var(--radius);
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
        }

        .logo-text h1 {
            font-size: 16px;
            font-weight: 700;
            letter-spacing: -0.3px;
        }

        .logo-text span {
            font-size: 11px;
            color: var(--text-muted);
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .sidebar-nav {
            flex: 1;
            padding: 16px 12px;
            overflow-y: auto;
        }

        .nav-section {
            margin-bottom: 24px;
        }

        .nav-section-title {
            font-size: 11px;
            font-weight: 600;
            color: var(--text-muted);
            text-transform: uppercase;
            letter-spacing: 1.2px;
            padding: 0 12px;
            margin-bottom: 8px;
        }

        .nav-link {
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 12px 14px;
            color: var(--text-secondary);
            text-decoration: none;
            border-radius: var(--radius-sm);
            transition: all 0.15s ease;
            font-weight: 500;
            font-size: 14px;
        }

        .nav-link:hover {
            background: var(--bg-card);
            color: var(--text-primary);
        }

        .nav-link.active {
            background: var(--accent-green);
            color: white;
        }

        .nav-link i {
            width: 20px;
            height: 20px;
            opacity: 0.8;
        }

        .nav-link.active i {
            opacity: 1;
        }

        .sidebar-footer {
            padding: 16px;
            border-top: 1px solid var(--border);
        }

        .user-info {
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 12px;
            background: var(--bg-card);
            border-radius: var(--radius-sm);
        }

        .user-avatar {
            width: 40px;
            height: 40px;
            background: linear-gradient(135deg, var(--accent-purple), var(--accent-blue));
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 600;
            font-size: 14px;
        }

        .user-details h4 {
            font-size: 14px;
            font-weight: 600;
        }

        .user-details span {
            font-size: 12px;
            color: var(--text-muted);
        }

        /* Main Content */
        .main {
            flex: 1;
            margin-left: 260px;
            min-height: 100vh;
        }

        .topbar {
            height: 64px;
            background: var(--bg-card);
            border-bottom: 1px solid var(--border);
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 0 32px;
            position: sticky;
            top: 0;
            z-index: 50;
        }

        .topbar-left {
            display: flex;
            align-items: center;
            gap: 16px;
        }

        .page-title {
            font-size: 20px;
            font-weight: 600;
        }

        .topbar-right {
            display: flex;
            align-items: center;
            gap: 16px;
        }

        .topbar-time {
            font-family: 'JetBrains Mono', monospace;
            font-size: 13px;
            color: var(--text-secondary);
        }

        .btn {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 10px 20px;
            border-radius: var(--radius-sm);
            font-weight: 600;
            font-size: 14px;
            border: none;
            cursor: pointer;
            transition: all 0.15s ease;
            text-decoration: none;
        }

        .btn-primary {
            background: var(--accent-green);
            color: white;
        }

        .btn-primary:hover {
            background: var(--accent-green-hover);
            transform: translateY(-1px);
        }

        .btn-secondary {
            background: var(--bg-card);
            color: var(--text-primary);
            border: 1px solid var(--border);
        }

        .btn-secondary:hover {
            background: var(--bg-card-hover);
        }

        .btn-danger {
            background: var(--accent-red);
            color: white;
        }

        .btn-sm {
            padding: 6px 12px;
            font-size: 13px;
        }

        .content {
            padding: 32px;
        }

        /* Cards */
        .card {
            background: var(--bg-card);
            border: 1px solid var(--border);
            border-radius: var(--radius);
            padding: 24px;
            margin-bottom: 24px;
        }

        .card-header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 20px;
        }

        .card-title {
            font-size: 16px;
            font-weight: 600;
        }

        /* Stats Grid */
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-bottom: 32px;
        }

        .stat-card {
            background: var(--bg-card);
            border: 1px solid var(--border);
            border-radius: var(--radius);
            padding: 20px 24px;
            display: flex;
            align-items: flex-start;
            gap: 16px;
        }

        .stat-icon {
            width: 48px;
            height: 48px;
            border-radius: var(--radius-sm);
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .stat-icon.green { background: rgba(0, 186, 124, 0.15); color: var(--accent-green); }
        .stat-icon.blue { background: rgba(29, 155, 240, 0.15); color: var(--accent-blue); }
        .stat-icon.orange { background: rgba(255, 122, 0, 0.15); color: var(--accent-orange); }
        .stat-icon.purple { background: rgba(120, 86, 255, 0.15); color: var(--accent-purple); }

        .stat-content h3 {
            font-size: 28px;
            font-weight: 700;
            letter-spacing: -0.5px;
            margin-bottom: 4px;
        }

        .stat-content span {
            font-size: 13px;
            color: var(--text-muted);
        }

        /* Tables */
        .table-container {
            overflow-x: auto;
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        th, td {
            padding: 14px 16px;
            text-align: left;
            border-bottom: 1px solid var(--border);
        }

        th {
            font-size: 12px;
            font-weight: 600;
            color: var(--text-muted);
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        td {
            font-size: 14px;
        }

        tr:hover td {
            background: var(--bg-card-hover);
        }

        /* Badges */
        .badge {
            display: inline-flex;
            align-items: center;
            padding: 4px 10px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 600;
        }

        .badge-success { background: rgba(0, 186, 124, 0.15); color: var(--accent-green); }
        .badge-warning { background: rgba(255, 212, 0, 0.15); color: var(--accent-yellow); }
        .badge-danger { background: rgba(244, 33, 46, 0.15); color: var(--accent-red); }
        .badge-info { background: rgba(29, 155, 240, 0.15); color: var(--accent-blue); }

        /* Forms */
        .form-group {
            margin-bottom: 20px;
        }

        .form-label {
            display: block;
            font-size: 13px;
            font-weight: 600;
            color: var(--text-secondary);
            margin-bottom: 8px;
        }

        .form-input, .form-select {
            width: 100%;
            padding: 12px 16px;
            background: var(--bg-dark);
            border: 1px solid var(--border);
            border-radius: var(--radius-sm);
            color: var(--text-primary);
            font-size: 14px;
            font-family: inherit;
            transition: border-color 0.15s ease;
        }

        .form-input:focus, .form-select:focus {
            outline: none;
            border-color: var(--accent-green);
        }

        .form-input::placeholder {
            color: var(--text-muted);
        }

        /* Alerts */
        .alert {
            padding: 16px 20px;
            border-radius: var(--radius-sm);
            margin-bottom: 20px;
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .alert-success { background: rgba(0, 186, 124, 0.15); border-left: 4px solid var(--accent-green); }
        .alert-error { background: rgba(244, 33, 46, 0.15); border-left: 4px solid var(--accent-red); }
        .alert-warning { background: rgba(255, 212, 0, 0.15); border-left: 4px solid var(--accent-yellow); }
        .alert-info { background: rgba(29, 155, 240, 0.15); border-left: 4px solid var(--accent-blue); }

        /* Clock Status */
        .clock-status {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 8px 16px;
            background: var(--bg-card);
            border-radius: 20px;
            font-size: 13px;
            font-weight: 500;
        }

        .clock-status .dot {
            width: 8px;
            height: 8px;
            border-radius: 50%;
        }

        .clock-status.working .dot { background: var(--accent-green); }
        .clock-status.break .dot { background: var(--accent-yellow); }
        .clock-status.out .dot { background: var(--text-muted); }

        /* Animations */
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(8px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .animate-in {
            animation: fadeIn 0.3s ease forwards;
        }

        /* Responsive */
        @media (max-width: 1024px) {
            .sidebar {
                transform: translateX(-100%);
            }
            .main {
                margin-left: 0;
            }
        }

        /* AI Assistant Panel */
        .ai-assistant-toggle {
            position: fixed;
            right: 24px;
            bottom: 24px;
            width: 44px;
            height: 44px;
            border-radius: 999px;
            border: none;
            display: flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(135deg, var(--accent-purple), var(--accent-blue));
            color: #fff;
            box-shadow: 0 8px 24px rgba(0, 0, 0, 0.35);
            cursor: pointer;
            z-index: 999;
        }

        .ai-assistant-toggle i {
            width: 20px;
            height: 20px;
        }

        .ai-assistant-panel {
            position: fixed;
            right: 24px;
            bottom: 80px;
            width: 340px;
            max-height: 70vh;
            background: var(--bg-card);
            border-radius: 16px;
            box-shadow: 0 18px 40px rgba(0, 0, 0, 0.55);
            display: none;
            flex-direction: column;
            overflow: hidden;
            z-index: 998;
            border: 1px solid var(--border);
        }

        .ai-assistant-panel.open {
            display: flex;
        }

        .ai-assistant-header {
            padding: 10px 14px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            border-bottom: 1px solid rgba(255, 255, 255, 0.06);
        }

        .ai-assistant-header-title {
            font-size: 14px;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .ai-assistant-header-title i {
            width: 18px;
            height: 18px;
        }

        .ai-assistant-messages {
            padding: 10px 12px;
            flex: 1;
            overflow-y: auto;
            display: flex;
            flex-direction: column;
            gap: 8px;
            font-size: 13px;
        }

        .ai-msg {
            padding: 8px 10px;
            border-radius: 12px;
            max-width: 90%;
            line-height: 1.4;
            white-space: pre-wrap;
            word-wrap: break-word;
        }

        .ai-msg-user {
            align-self: flex-end;
            background: rgba(0, 186, 124, 0.15);
        }

        .ai-msg-assistant {
            align-self: flex-start;
            background: rgba(120, 86, 255, 0.12);
        }

        .ai-assistant-input {
            padding: 8px 10px;
            border-top: 1px solid rgba(255, 255, 255, 0.06);
            display: flex;
            gap: 8px;
        }

        .ai-assistant-input input {
            flex: 1;
            border-radius: 999px;
            border: 1px solid rgba(255, 255, 255, 0.12);
            background: rgba(0, 0, 0, 0.3);
            color: var(--text-primary);
            padding: 8px 12px;
            font-size: 13px;
        }

        .ai-assistant-input button {
            border-radius: 999px;
            border: none;
            padding: 8px 12px;
            font-size: 13px;
            background: var(--accent-green);
            color: #000;
            cursor: pointer;
        }
    </style>
    {% block extra_css %}{% endblock %}
</head>
<body>
    <!-- Sidebar -->
    <aside class="sidebar">
        <div class="sidebar-header">
            <div class="logo">
                <div class="logo-icon">
                    <i data-lucide="flower-2" style="width: 24px; height: 24px;"></i>
                </div>
                <div class="logo-text">
                    <h1>Mountain Gardens</h1>
                    <span>POS System</span>
                </div>
            </div>
        </div>

        <nav class="sidebar-nav">
            <div class="nav-section">
                <div class="nav-section-title">Main</div>
                <a href="{{ url_for('dashboard') }}" class="nav-link {% if request.endpoint == 'dashboard' %}active{% endif %}">
                    <i data-lucide="layout-dashboard"></i>
                    Dashboard
                </a>
                <a href="{{ url_for('sales') }}" class="nav-link {% if 'sales' in request.endpoint %}active{% endif %}">
                    <i data-lucide="shopping-cart"></i>
                    Sales Terminal
                </a>
            </div>

            <div class="nav-section">
                <div class="nav-section-title">Inventory</div>
                <a href="{{ url_for('inventory') }}" class="nav-link {% if request.endpoint == 'inventory' %}active{% endif %}">
                    <i data-lucide="package"></i>
                    Items
                </a>
                <a href="{{ url_for('low_stock_report') }}" class="nav-link {% if request.endpoint == 'low_stock_report' %}active{% endif %}">
                    <i data-lucide="alert-triangle"></i>
                    Low Stock
                </a>
                <a href="{{ url_for('receive_stock') }}" class="nav-link {% if request.endpoint == 'receive_stock' %}active{% endif %}">
                    <i data-lucide="package-plus"></i>
                    Receive Stock
                </a>
            </div>

            <div class="nav-section">
                <div class="nav-section-title">Time & HR</div>
                <a href="{{ url_for('timeclock') }}" class="nav-link {% if request.endpoint == 'timeclock' %}active{% endif %}">
                    <i data-lucide="clock"></i>
                    Time Clock
                </a>
                <a href="{{ url_for('timeoff') }}" class="nav-link {% if request.endpoint == 'timeoff' %}active{% endif %}">
                    <i data-lucide="calendar-off"></i>
                    Time Off
                </a>
            </div>

            <div class="nav-section">
                <div class="nav-section-title">Reports</div>
                <a href="{{ url_for('daily_report') }}" class="nav-link {% if request.endpoint == 'daily_report' %}active{% endif %}">
                    <i data-lucide="bar-chart-3"></i>
                    Daily Summary
                </a>
                <a href="{{ url_for('employee_dashboard') }}" class="nav-link {% if request.endpoint == 'employee_dashboard' %}active{% endif %}">
                    <i data-lucide="trophy"></i>
                    Leaderboard
                </a>
                <a href="{{ url_for('inventory_report') }}" class="nav-link {% if request.endpoint == 'inventory_report' %}active{% endif %}">
                    <i data-lucide="warehouse"></i>
                    Inventory Value
                </a>
            </div>

            {% if session.get('role') in ['Manager', 'Owner', 'Admin'] %}
            <div class="nav-section">
                <div class="nav-section-title">Admin</div>
                <a href="{{ url_for('employees') }}" class="nav-link {% if 'employee' in request.endpoint %}active{% endif %}">
                    <i data-lucide="users"></i>
                    Employees
                </a>
                <a href="{{ url_for('pricing_rules') }}" class="nav-link {% if request.endpoint == 'pricing_rules' %}active{% endif %}">
                    <i data-lucide="tag"></i>
                    Pricing Rules
                </a>
                <a href="{{ url_for('audit_log') }}" class="nav-link {% if request.endpoint == 'audit_log' %}active{% endif %}">
                    <i data-lucide="shield"></i>
                    Audit Log
                </a>
            </div>
            {% endif %}
        </nav>

        <div class="sidebar-footer">
            <div class="user-info">
                <div class="user-avatar">
                    {{ session.get('employee_name', 'U')[0] }}
                </div>
                <div class="user-details">
                    <h4>{{ session.get('employee_name', 'User') }}</h4>
                    <span>{{ session.get('role', 'Staff') }}</span>
                </div>
            </div>
            <a href="{{ url_for('logout') }}" class="btn btn-secondary btn-sm" style="width: 100%; margin-top: 12px; justify-content: center;">
                <i data-lucide="log-out" style="width: 16px; height: 16px;"></i>
                Sign Out
            </a>
        </div>
    </aside>

    <!-- Main Content -->
    <main class="main">
        <header class="topbar">
            <div class="topbar-left">
                <h2 class="page-title">{% block page_title %}Dashboard{% endblock %}</h2>
            </div>
            <div class="topbar-right">
                <span class="topbar-time" id="clock"></span>
                {% block topbar_actions %}{% endblock %}
            </div>
        </header>

        <div class="content">
            {% with messages = get_flashed_messages(with_categories=true) %}
                {% if messages %}
                    {% for category, message in messages %}
                        <div class="alert alert-{{ category }} animate-in">
                            <i data-lucide="{% if category == 'success' %}check-circle{% elif category == 'error' %}x-circle{% else %}info{% endif %}" style="width: 20px; height: 20px;"></i>
                            {{ message }}
                        </div>
                    {% endfor %}
                {% endif %}
            {% endwith %}

            {% block content %}{% endblock %}
        </div>
    </main>

    {% if session.get('employee_id') %}
    <!-- AI Assistant Panel -->
    <div id="aiAssistantPanel" class="ai-assistant-panel">
        <div class="ai-assistant-header">
            <div class="ai-assistant-header-title">
                <i data-lucide="sparkles"></i>
                <span>AI Assistant</span>
            </div>
            <button id="aiAssistantClose" class="btn btn-sm btn-secondary" type="button">
                <i data-lucide="x" style="width: 14px; height: 14px;"></i>
            </button>
        </div>
        <div id="aiAssistantMessages" class="ai-assistant-messages">
            <div class="ai-msg ai-msg-assistant">
                Hi {{ session.get('employee_name', 'there') }} 👋
                Ask me about this screen, products, or how to do something in the POS.
            </div>
        </div>
        <form id="aiAssistantForm" class="ai-assistant-input">
            <input id="aiAssistantInput" type="text" autocomplete="off"
                   placeholder="Ask a question...">
            <button type="submit">
                Send
            </button>
        </form>
    </div>

    <button id="aiAssistantToggle" class="ai-assistant-toggle" type="button" title="AI Assistant">
        <i data-lucide="sparkles"></i>
    </button>
    {% endif %}

    <script>
        // Initialize Lucide icons
        lucide.createIcons();

        // Live clock
        function updateClock() {
            const now = new Date();
            const options = { weekday: 'short', month: 'short', day: 'numeric', hour: '2-digit', minute: '2-digit', second: '2-digit' };
            document.getElementById('clock').textContent = now.toLocaleDateString('en-US', options);
        }
        updateClock();
        setInterval(updateClock, 1000);
    </script>

    <!-- AI Assistant JS -->
    <script>
        (function () {
            const toggleBtn = document.getElementById('aiAssistantToggle');
            const panel = document.getElementById('aiAssistantPanel');
            const closeBtn = document.getElementById('aiAssistantClose');
            const form = document.getElementById('aiAssistantForm');
            const input = document.getElementById('aiAssistantInput');
            const messagesEl = document.getElementById('aiAssistantMessages');

            if (!toggleBtn || !panel || !form || !input || !messagesEl) {
                return;
            }

            let conversationId = null;
            window.AI_CONTEXT = window.AI_CONTEXT || {};

            function scrollToBottom() {
                messagesEl.scrollTop = messagesEl.scrollHeight;
            }

            function appendMessage(role, text) {
                const div = document.createElement('div');
                div.classList.add('ai-msg');
                div.classList.add(role === 'user' ? 'ai-msg-user' : 'ai-msg-assistant');
                div.textContent = text;
                messagesEl.appendChild(div);
                scrollToBottom();
            }

            function openPanel() {
                panel.classList.add('open');
                input.focus();
            }

            function closePanel() {
                panel.classList.remove('open');
            }

            toggleBtn.addEventListener('click', () => {
                if (panel.classList.contains('open')) {
                    closePanel();
                } else {
                    openPanel();
                }
            });

            closeBtn.addEventListener('click', closePanel);

            form.addEventListener('submit', async (e) => {
                e.preventDefault();
                const text = input.value.trim();
                if (!text) return;

                appendMessage('user', text);
                input.value = '';
                input.focus();

                const payload = {
                    message: text,
                    conversation_id: conversationId,
                    context: Object.assign({}, window.AI_CONTEXT || {}, {
                        path: window.location.pathname
                    })
                };

                try {
                    const resp = await fetch('/ai/chat', {
                        method: 'POST',
                        headers: {'Content-Type': 'application/json'},
                        body: JSON.stringify(payload)
                    });

                    const data = await resp.json();
                    if (data.conversation_id) {
                        conversationId = data.conversation_id;
                    }

                    if (data.reply) {
                        appendMessage('assistant', data.reply);
                    } else if (data.error) {
                        appendMessage('assistant', 'Error: ' + data.error);
                    } else {
                        appendMessage('assistant', 'I did not get a response from the AI service.');
                    }
                } catch (err) {
                    appendMessage('assistant', 'Network error talking to AI service.');
                }
            });

            // Allow other pages to open the assistant with extra context
            window.openAssistantWithContext = function (seedMessage, extraContext) {
                window.AI_CONTEXT = Object.assign({}, window.AI_CONTEXT || {}, extraContext || {});
                openPanel();
                if (seedMessage) {
                    input.value = seedMessage;
                    input.focus();
                }
            };
        })();
    </script>

    {% block extra_js %}{% endblock %}
</body>
</html>
