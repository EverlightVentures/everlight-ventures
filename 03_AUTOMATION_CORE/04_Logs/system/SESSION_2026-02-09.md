# Session Log — Feb 9-10, 2026 (Claude Opus 4.6)

## Summary
Major session covering Gemini CLI install, master dashboard OOM fix, XLM bot diagnosis/repair/live deployment, and XLM dashboard overhaul.

---

## 1. Google Gemini CLI Installed

- Installed `@google/gemini-cli` v0.27.2 globally via npm
- Binary at `/usr/local/bin/gemini`
- Alias `gm='gemini'` added to `~/.zshrc`
- API key still needs to be added to `/root/.config/ai/keys.env` (user will do when they get the key)

**Files modified:**
- `~/.zshrc` — added `gm` alias, `ask` alias, updated banner to show all AI tools

---

## 2. Smart AI Router Created

Created `/mnt/sdcard/AA_MY_DRIVE/03_AUTOMATION_CORE/01_Scripts/ai_workers/ask_router.py`

Routes queries to the best AI based on pattern matching:
- Research/news/prices → `ppx` (Perplexity)
- Code generation → `gm` (Gemini)
- Codebase analysis → `cl` (Claude)
- General → `ai` (GPT)

Supports `--tool` flags to force a specific AI. Alias: `ask "your question"`

---

## 3. Master Dashboard OOM Fix (Exit Code 9)

**Root cause:** Dashboard at `/mnt/sdcard/AA_MY_DRIVE/09_DASHBOARD/master_dashboard/` was getting OOM-killed.

**5 issues fixed:**
1. Removed `--reload` flag from uvicorn (file watchers on entire drive)
2. Capped index at 5,000 items (`MAX_INDEX_ITEMS = 5000` in app.py)
3. Added nested root skip to prevent double-indexing ProtonDrive
4. Changed `index_refresh_sec` from 60 → 300 in config.json
5. Split requirements: lightweight `requirements.txt` for dashboard, `requirements-analytics.txt` for heavy deps

**Files modified:**
- `09_DASHBOARD/master_dashboard/master_restart.sh`
- `09_DASHBOARD/master_dashboard/app.py`
- `09_DASHBOARD/master_dashboard/config.json`
- `09_DASHBOARD/master_dashboard/requirements.txt` (slimmed)
- `09_DASHBOARD/master_dashboard/requirements-analytics.txt` (new)
- `09_DASHBOARD/master_dashboard/analytics_run.sh`
- `09_DASHBOARD/master_dashboard/master_launch.sh`

---

## 4. XLM Bot — Diagnosis & Repair

**Root cause of zero trades (6,240 decisions, 0 entries):**
1. `data/candles.py` — `load_or_fetch()` returned stale CSV forever (only 49 candles from Feb 2, never refreshed)
2. ATR multiplier 1.1 too high for XLM (needed < 1.0)
3. RSI band 48-52 absurdly narrow (widened to 40-60)
4. RVOL threshold 1.5 too high (lowered to 1.2)
5. EMA(200) required but only 49 candles available (made graceful degradation)

**Fixes applied:**

### candles.py (`xlm_bot/data/candles.py`)
- Added `_is_stale()` check — detects CSVs older than 30min
- Added paginated `fetch_coinbase_candles()` — loops 300-candle chunks with 0.3s rate limit
- `load_or_fetch()` now always refreshes stale data and rebuilds higher timeframes from 15m base

### confluence.py (`xlm_bot/strategy/confluence.py`)
- `ema_bias()` — gracefully degrades when <200 candles (only applies filter when enough data)
- `rsi_ok()` — widened from 48-52 to 40-60, lookback thresholds from 40/60 to 30/70
- `rvol_ok()` — min_rvol from 1.5 to 1.2
- `structure_zone()` — tolerance from 0.005 to 0.008
- `fib_zone()` — tolerance from 0.003 to 0.005

### config.yaml (`xlm_bot/config.yaml`)
- `paper: false` (was true)
- `atr_multiplier: 0.85` (was 1.1)
- `reserve_usd: 50` (was 300)
- `spot_reserve_floor_usd: 50` (was 200)
- `max_transfer_per_day_usd: 500` (was 300)

### xpb.sh (`xlm_bot/xpb.sh`)
- Added `while true; do ... sleep 60; done` loop (was missing, only ran once)

### Data cleanup
- Deleted stale CSV files in `xlm_bot/data/` to force fresh fetch

---

## 5. XLM Bot — Live Deployment

- Bot running LIVE as PID 29545: `while true; do python main.py --live --i-understand-live; sleep 60; done`
- User funded account with $226
- Bot is seeing signals (up to 5/6 confluences, SHORT breakout_retest and pullback patterns)
- ATR regime gate is the main blocker during low-volatility periods — this is by design for leveraged trading

---

## 6. XLM Dashboard — Complete Overhaul

**Location:** `xlm_bot/dashboard.py` running on `localhost:8502` via Streamlit

### What was removed (redundancy cleanup):
- Duplicate equity/buying power (was shown 2x)
- Duplicate open position (was shown 2x)
- Duplicate recent trades (was shown 2x)
- Duplicate confluence/gates display (was shown 4x!)
- Contract specs section (static, not actionable)
- Profit siphon section (no transfers yet)
- Funding status section (merged into balance)

### Final layout (9 sections, zero redundancy):
1. **Balance** — equity + buying power (Coinbase API)
2. **Celebration Banner** — green highlight when trade closes in profit
3. **Scoreboard** — Signals Met (X/6), Direction, Wins, Losses, Win Rate, Total P&L
4. **What's Happening Now** — plain English status + gate pills
5. **Market Intelligence** — price trend, gate flips, signal changes with timestamps
6. **Missed Trades Analysis** — each blocked signal with hypothetical P&L + verdict
7. **Parameter Insight** — which gate blocks the most, what to adjust, where in config
8. **Recent Log** — last 5 decisions as one-liners
9. **Open Position + Live P&L** — only appears when in a trade
10. **Trade Log** — entries + exits with dollar P&L
11. **Price Chart** — last 200 ticks

### Key features:
- All times in Pacific Time (UTC-8), not UTC
- Auto-refreshes every 60 seconds via meta refresh tag
- Reads from: `decisions.jsonl`, `trades.csv`, `state.json`, Coinbase API
- Celebration banner auto-shows on profitable trade closes
- Missed trade analyzer compares what would have happened if blocked signals were taken
- Parameter insight tells you exactly which config value to change

---

## 7. User Preferences (saved to `~/.claude/CLAUDE.md`)

- Timezone: Pacific Time (UTC-8)
- Prefers plain English over technical jargon on dashboards
- Plans to scale XLM bot to $1,000-$2,000 for $100 scalps
- Config `risk_pct: 0.05` and `target_pct: 0.10` auto-scale with balance (no param changes needed)

---

## Current State

### Running processes:
- XLM bot LIVE: `while true; do python main.py --live --i-understand-live; sleep 60; done`
- XLM dashboard: Streamlit on `localhost:8502`

### Bot status:
- Seeing SHORT signals (breakout_retest, pullback) with 3-5/6 confluences
- ATR regime gate is primary blocker during low-vol consolidation
- No margin issues (user funded $226, config reserves lowered to $50)
- 0 trades executed so far — waiting for volatility expansion

### AI CLI ecosystem:
| Alias | Tool | Status |
|-------|------|--------|
| `ai` | GPT | Working |
| `cx` | Codex | Working |
| `ppx` | Perplexity | Working |
| `gm` | Gemini | Installed, needs API key |
| `cl` | Claude | Working |
| `ask` | Smart Router | Created, working |

### Key file paths:
- XLM bot: `/mnt/sdcard/AA_MY_DRIVE/xlm_bot/`
- XLM config: `/mnt/sdcard/AA_MY_DRIVE/xlm_bot/config.yaml`
- XLM decisions: `/mnt/sdcard/AA_MY_DRIVE/xlm_bot/logs/decisions.jsonl`
- XLM trades: `/mnt/sdcard/AA_MY_DRIVE/xlm_bot/logs/trades.csv`
- XLM dashboard: `/mnt/sdcard/AA_MY_DRIVE/xlm_bot/dashboard.py`
- Master dashboard: `/mnt/sdcard/AA_MY_DRIVE/09_DASHBOARD/master_dashboard/`
- AI router: `/mnt/sdcard/AA_MY_DRIVE/03_AUTOMATION_CORE/01_Scripts/ai_workers/ask_router.py`
- User prefs: `~/.claude/CLAUDE.md`
