# ALLEY KINGZ -- SYSTEMS DESIGN (research-verified against live code, 2026-06-19)
> Output of the systems-research workflow (wf_633c9382, 14 agents, verified against live game/engine.js + economy.js). The authoritative buildable systems spec. Companion to AK_GAME_VISION.md (north-star) + AK_V2_BUILD_SPEC.md (V2 builds). Full raw result archived in the run transcript.

## ARCHITECTURE (one sentence)
ONE combat brain (engine.js) + ONE combo brain (a kernel extracted from engine.js) feed FOUR renderers (battler / base-defense / raid / overworld), gated by ONE meta number (MAIN TOWER = the Town Hall), instrumented by ONE additive per-entity sensor struct + ONE event bus, all on a SOFT-currency-only economy walled off from $BCARDD/ALK. = exactly the operator's "one consistent system, per-mode customizable menus."

## CRITICAL CORRECTION (verified by reading the code -- supersedes the V2 spec's reuse claim)
ALLEY_KINGZ_CORE/ (EventBus, DataValidator, AntiCheatValidator, BuildingBase, RaidController, CurrencyManager, TokenSink) is a PARALLEL scaffold NOT wired into the live game (grep of game/ for EventBus = ZERO hits). The raid stack is STUB ("logic is TODO"). So: base-defense, raids, server-authority, and the ALK token are GREENFIELD (build net-new) -- NOT reuse. TRUE reuse = only the battler (engine.js), the combo system, the level curve, and the soft-currency ledger (economy.js). Plan accordingly: tune+gate+instrument what is live; build the city/raid/economy-token loop net-new on the same kernels.

## VERIFIED LIVE CONSTANTS (tune these, don't invent)
ENERGY_MAX=10, ENERGY_RATE=1/1.8s (effective = *secMult*combatScale*eMod*pReg; AI dampened aiReg=0.78+DIFFICULTY*0.052), ENERGY_SECTION_MULT=[1.0,1.2,1.4,2.0], energyCost=round(2+(canonCost-2)*7/9), akLevelMult=1+0.06*(lv-1) cap L10 (+54%), UP_COINS{C20/R50/E400/L600/M1000}, CARD_LV_CAP=10, CAP_COINS=40/match, CAP_SHARDS=10/match, computeSynergy + computeNamedSynergy (21 named combos, SYNERGY_MIN=3, MOVE_CAP=2.0/DMG_CAP=1.8), 4-card cycle (dealHand=deck.slice(0,4)), SEP_VIS_R=0.92, NS_HEAL_R=3.0, ENGAGE_STOP/RESUME=0.95/1.10, tower ranges 6/6.5, two sanctioned outrangers (Laser Beagle 6.5, Rail Terrier 6.25).

## THE 5 SYSTEMS (design + reuse-vs-greenfield)
1. CARD-DEPLOY x TOWN HALL: two deploy surfaces, one gate. Battler ENERGY is live + correct (don't rebuild; only add a next-card HUD PREVIEW). MAIN TOWER = Town Hall = one number gating card-level cap / production buildings / locked districts / crew size / a NEW raid "Convoy Capacity" (army-camp slot budget, housing = canon cost). HIGHEST-LEVERAGE ANTI-WHALE: one-line economy.js cardLevel()=min(level, MainTowerLevel). Ranked "tournament standard" normalizes all cards to L9 so wallet != win. REUSE: energy/cycle/level/upgrade economy. GREENFIELD: the building/upgrade-timer loop.
2. CARD COMBOS (one kernel, four renderers): EXTRACT computeSynergy + computeNamedSynergy into a pure combo_kernel.js with resolve(roster,context); engine.js calls it byte-identical (prove via the headless harness). Same kernel serves battler / base-defense / raid (resolve BOTH rosters) / overworld (dominant-faction HUD passive). Add only ~3 surface combos (HOME TURF/WATCHDOGS/SIEGE). Clamp combo damage AFTER the anti-whale cap in raids. REUSE: kernel extraction (byte-identical). GREENFIELD: the defense/raid combat that will call it.
3. BREEDING / HYBRIDS (The Kennel): FIXED-ROSTER DISCOVERY (Dragon City / Monster Legends), NOT Axie's infinite generator. Pick 2 parents -> egg -> incubate -> hatch a hybrid that is just a cards.json object made by the EXISTING balance formula (_balance_pass.py) -> no per-breed art, finite supply, born in-budget. Offspring stats DECOUPLED from parent power. Mythics never breed/output. Published odds + PITY counter. NET soft-currency SINK (burns Bones; release-overflow). REUSE: balance formula + cards render path + Bones sink + hub primitives. GREENFIELD: Kennel building + breeding screen + roll edge fn + incubation FSM.
4. WILD ENCOUNTERS + DJINN: SYMBOL encounters (visible/avoidable, never forced-random) roam the hub; walk in -> encounter card shows faction/tier/matchup UP FRONT -> routes into the LIVE battler via ?mode=encounter + localStorage round-trip (no new combat scene). CAPTURE (Monster Hunter): below an HP threshold + a Leash, server-computed chance. DJINN = Bones/Charms (Golden Sun) reusing AK.fireSpecial/game.special + the live SKILL_TREE; 3 states SET/STANDBY/RECOVERY, all fields default-falsy. 4-faction RPS x2/x0.5. REUSE: battler + seeded PRNG + hub primitives + skill-tree. GREENFIELD: roamers[] + ENCOUNTER state + wander/chase/detect radii (MUST add chaseLeashR or strays grief forever) + capture edge fn.
5. UNIFIED ECONOMY: SHIP 5 currencies not 8 (never a currency before its sink loop; Supercell cut CR to 3 in 2025). LAUNCH: Coins/Gold (bottleneck), Copies, Scrap (pity/deterministic Card Shop), Keys, Bones (soulbound, skill-tree fuel, never gem-buyable). DEFER NOS (city loop) + ALK (prestige/social) until their loops exist. Gems (live Stripe) buy TIME + cosmetics, never power. REUSE: window.AK_ECON (coins/scrap/keys/copies, atomic mutateProfile, caps, SCRAP_DUPE). GREENFIELD: server-authoritative grant ledger (economy is localStorage today); CurrencyManager/TokenSink are stubs.

## SENSOR PACKAGES + TELEMETRY ("sensor packages for everything")
ONE additive per-entity struct {detectR, visionR, strikeR, sepR, aoeR}, all new fields default-falsy. TWO-SENSOR awareness (LoL turret rule): vision/detect radius is SEPARATE from + LARGER than strike radius (sees before it hits); cap any sensor to one phone screen. Expose live battler ranges READ-ONLY; add detectR/visionR only for hub strays + raid towers. FREEZE the dot-namespace event taxonomy in DataValidator (match.start, sink.spend, source.grant -- NOT snake_case). 6 lanes: funnel / combat-balance / economy / social-virality / anti-cheat / perf. PERF: spatial-grid the detection scan, rescan aggro at 5-10Hz (never per-frame hypot), pre-render glows to sprites (NEVER strip -- operator veto). Batch -> Supabase edge fn on mfghdobptredxxhbjwyz ONLY -> rollup tables. SERVER AUTHORITY at the LEDGER (grants), not full combat re-sim; until the verifier is live, every client number is an untrusted CLAIM.

## CRYPTO GUARDRAIL (single hard wall, all systems)
Soft-currency-only economy (Gold/Copies/Scrap/Keys/Bones, all faucet-capped per-match + per-day); $BCARDD is NEVER an earn-by-play reward and Gold has NO redemption path to it. $BCARDD/ALK cosmetic + geo-gated only -- decoupled from gameplay AND the cosmetic-purchase rail (buy cosmetics with fiat/soft currency, not the token). Gambling firewall: random chests open only with EARNED soft currency; any gem purchase is DETERMINISTIC (see-what-you-buy), publish odds, pity counter, age-gate. Axie firewall on breeding: offspring soft/non-tradeable/non-cashable/zero-yield, stats decoupled, net sink. Securities gates DEFERRED: ALK staking fee-share + ALK<->$BCARDD bridge OFF until a securities attorney signs off; strip "expectation of profit" language from all player-facing surfaces. All economy writes via edge fns on mfghdobptredxxhbjwyz (never jdqqmsmwmbsnlnstyavl).

## BUILD SEQUENCE (after the in-progress Build 2)
P0 (prereq): wire EventBus + DataValidator + ConfigLoader into live index.html/engine.js as a READ-ONLY emit bridge; freeze the taxonomy; add the per-entity sensor struct (default-falsy). [the integration step the research flagged as not-yet-done]
S1: extract combo_kernel.js (byte-identical, unblocks every surface).
S2: anti-whale meta gate -- one-line cardLevel() Main-Tower clamp + next-card HUD preview + Convoy Capacity number. (highest leverage, lowest cost)
S3: wild encounters Surface 3 (rides Build 2 roaming AI) -- roamers[] + ENCOUNTER state + radii + ?mode=encounter + overworld faction HUD passive.
S4: Djinn/Bones loadout (SET/STANDBY/RECOVERY, reuse fireSpecial + skill-tree).
S5: 5-currency consolidation + server-authoritative grant ledger (mfghdobptredxxhbjwyz). Defer NOS + ALK.
S6: GREENFIELD raid + base-defense combat loop (= the foundation of the Brawl-Stars x CoC x mini-map battle-hybrid): BuildingBase HP/level/timers -> RaidController/DamageCalculator/ShieldSystem -> call combo_kernel for defense+siege.
S7: The Kennel breeding (after the ledger + collection are solid).
S8: server-authority hardening (AntiCheatValidator.setVerifier) for ranked/crew-war/ALK modes only.
S9 (DEFERRED, legal-gated ~Q4'26): ALK CurrencyManager + TokenSink + the ALK<->$BCARDD bridge, OFF until attorney sign-off.

## DOUBLE-CHECK (the 2 verify agents that dropped on connection -- reviewed manually 2026-06-19)
- breeding_hybrid: BUILDABLE + CRYPTO-SAFE. Reuses the balance formula + cards render path + Bones sink; net-new = the Kennel building + roll edge fn + incubation FSM (clear scope). Axie firewall explicit (fixed-roster, non-tradeable, net sink, Mythics never breed, stats decoupled). Verdict stands; the verify agent failed on connection, not substance.
- wild_encounters: BUILDABLE + CRYPTO-SAFE. Reuses the live battler (?mode=encounter) + hub primitives + skill-tree; net-new = roamers[] + ENCOUNTER state + radii + capture edge fn. Symbol-encounters (visible/avoidable) is the correct mobile call. MUST add chaseLeashR (flagged) or strays grief forever. Captures = soft collectibles, non-tradeable. Verdict stands.

## SECOND WORKFLOW (monetization + world-combat + skill-trees + workers + fortress -- wf_03fd21bd, 2026-06-19)
CRITICAL FINDING: two verifiers flipped crypto_safe FALSE on the LIVE ALK-priced shields (ShieldSystem.js costAlk 100/250/700 + AK_RAID_DEFENSE_SYSTEM ALK shield tiers). A loot-protection shield priced in the TOKEN = utility-for-token = the pay-to-win/securities violation. THE LOAD-BEARING FIX (Build sequence STEP 0, zero-dependency, unblocks everything): reprice every gameplay-utility SKU off $BCARDD/ALK onto soft currency (gold/gems/fiat); delete the ALK-cosmetic-bypass; the token is a COSMETIC-ONLY lane. PARITY INVARIANT (binding, bake into DataValidator/economy): gems/$BCARDD may ONLY skip a production/upgrade TIMER -- never raise the rate, cap, or fragment/skill-point ceiling (Card Forge + Research Lab feed combat power, so timer-skip-only keeps it not-pay-to-win). Contract-first: each system = a registry in the handlers_data.js schema ({id,name,effect,bones,requires,mods} -- live key is 'bones' not 'cost'), all-new fields default-falsy (zero-state = byte-identical).
SYSTEMS: (1) THE BLOCK MARKET -- one product contract {id,sku,category,currency(NEVER token for utility),effect,duration,cooldown,geoGate}; 4 families: 5-tier Shields (gold/gems/fiat), Repel (encounter opt-out consumable), Potions (Kibble Rush time-skip / Infirmary heal+recruit / Patch revert), Skins (the ONLY token-eligible lane). (2) STREET WAR -- world-map real-time MOBA mode (health/energy/mana taskbar), a MODE skin over the sim, never fork engine.js, DEFERRED to its own research pass after the battle-hybrid. (3) COLLAR CONSTELLATIONS -- skill trees on ONE shared pattern (per-card "Collar Constellation" + the 6 live handler Bones trees + Dragon-Age 1-of-3 Specialization fork); quick win = a VISIBLE attribute sheet (base->tuned deltas). (4) THE CREW ON SHIFT -- deck-as-workers (assignedTo + Town-Hall worker cap + staff the 5 FACTORY ROW buildings + offline production/decay); parity-bound. (5) THE FORTRESS -- night-defense + the new wood/stone loot economy (materials -> walls/barricades + Infirmary heal/recruit); reprice shields off ALK first.
BUILD SEQUENCE: 0 crypto gate (reprice shields + parity invariant) -> 1 Block Market (Repel/Potions/repriced shields/skins) -> 2 Fortress wood/stone loot (Build-2 completion) -> 3 Crew-on-Shift deck-as-workers MVP -> 4 Collar Constellations skill trees (visible sheet -> Mastery faucet -> exemplar trees -> template) -> 5 Build-3 raids + bot bases (ak-bot-seed FIRST) -> 6 Street War MOBA (own pass) -> 7 cosmetics/art backfill (never gates a feature).
3 STRUCTURAL DECISIONS -- LOCKED 2026-06-19 (operator): (1) DECK SIZE = 8-card tower deck (live, Clash-Royale-standard, NO rebalance). The CITY WORKFORCE is a SEPARATE, larger roster drawn from the full collection -- the "11"/more lives THERE, never in the tower deck. (2) FACTIONS = TWO LAYERS: combat-faction (Boneguard/Zoomie/K9/Leashbreak, LIVE, drives the 21 battle combos -- do NOT touch) + lore-faction (Crowned/Rusted/Hologhosts/Unbound, world/clan allegiance + territory flavor); every card carries BOTH (class + tribe). (3) namespace workforce.*/crew-roster.* distinct from SQUAD/warlane.* (architect resolves). SCHEMA CAN NOW FREEZE. Full raw result in the run transcript.
