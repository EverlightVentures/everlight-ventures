Langfuse runs on Oracle via `docker compose`.

Use `../deploy_oracle_observability.sh langfuse` to upload the compose file, create a remote `.env` if missing, and start the stack with `sudo docker compose`.

Default URL: `http://<oracle-ip>:3100`
