# mdtv -> moved

**This empty scaffold was superseded on 2026-05-31 by the `hive_tv` project.**

The media / TV aggregation work (now called **Hive TV**: unified streaming + live guide +
movie wall + Linux gaming on the AceMagician) lives at:

    06_DEVELOPMENT/hive_tv/

Specs: `06_DEVELOPMENT/hive_tv/docs/specs/`

This folder is kept only as a pointer to avoid duplicate media directories. Safe to delete
once you have confirmed nothing references `mdtv`.
