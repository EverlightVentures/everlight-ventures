# ALLEY KINGZ -- V2 BUILD SPEC (research-backed, 2026-06-19)
> Output of the 13-agent research workflow (top-game patterns + latest tech, adversarially verified, tailored to AK: dogs/crypto/tower-defense/world-map/social). Build order locked by operator: 1 multi-map -> 2 night-raids+movement -> 3 world-map+bots, ONE at a time. This is the build bible; AK_LIVING_WORLD.md + AK_RAID_DEFENSE_SYSTEM.md are the design canon.

## TECH DECISION (hard laws threaded through all 3 builds)
- STAY 2.5D Canvas2D for ALL of V2. Reuse the live hub (index.html == hub_proto.html, edit both) and engine.js battler BYTE-FOR-BYTE. Never rebuild. Three.js = V3.
- All 3 builds are ADDITIVE data-driven layers on existing primitives (#fade 450ms, #banner, #dist title card, radar, spawnGrace, entering-lock). Hand-rolled 3-state FSM (IN_ZONE / TRANSITIONING / LOCKED_GATE), no npm. Only location.href crosses to a separate app (game.html, shop.html); zone+world travel are IN-PAGE state swaps.
- PERFORMANCE LAW (operator veto: never strip glows): PRE-RENDER every glow to a cached offscreen sprite (41 game.html + 6 hub shadowBlur = the #1 mobile FPS killer). Object-pool the horde, cap concurrent agents + particles, DPR<=2 + frame-time watchdog (drop to DPR=1 + half embers on sustained >20ms frames). Protect FX on $100 Androids, never strip globally.
- SERVER BOUNDARY: all multiplayer/economy state on AK Supabase **mfghdobptredxxhbjwyz** (NEVER jdqqmsmwmbsnlnstyavl = broker/everlightventures.io; AUTH separation). Edge-function-authoritative (copy alley-kingz-shop intent-POST), RLS forced, anon read-only, PII never on the public maps CDN.
- CRYPTO BOUNDARY (HARD LAW): combat/raid/territory rewards + shields + repairs = play-for-fun SOFT currency ONLY (gold/gems/bones). $BCARDD/ALK = geo-gated COSMETIC/identity layer -- never loot, never stipend, never paid PvP shield, never power-gate (Howey/gambling/MSB). No token rides a client-reported outcome. Theo GC sign-off before any redemption rail.
- REUSE ALLEY_KINGZ_CORE spine (EventBus/ConfigLoader/SaveLoadManager + pure deterministic DamageCalculator/ShieldSystem/RaidController, 38/38 tests) instead of fresh server math. The ONE net-new integration surface = a read-only engine.js EventBus "raid/siege mode" adapter (emits match.start/win/lose). Bounded, never a battler fork.
- ART: Leonardo API DEAD since 2026-06-10 (CF Workers AI failover needs CF_AI_TOKEN). Ship every feature on live glyph/procedural fallbacks; do NOT gate any launch on art generation.
- DEPLOY: only via sole-deployer e5 ~/ak_deploy -> ship.sh foreground (timeout 540, cf_pages_direct_upload --exclude assets/maps,assets/hub), verify on live CDN edge (~30-60s) in a real browser (Playwright on e5), never tool exit.
- CORRECTIONS the research caught: only 4 painted cities exist (the_lot/neon_night/golden_industrial/rain_docks), NOT 10. The 14 hub assets ARE wired in-proto. Matchmaking = M08 scout pool, not new M03. engine EventBus raid-mode adapter is net-new. Kingdom Rush has NO adaptive spawner -> use scripted escalating waves.

## BUILD 1 -- MULTI-MAP DISTRICTS (real discrete zones) [BUILD FIRST]
Pattern: Stardew location/warp (warp lands >=1 tile deeper, kills bounce) + Sunflower hub-and-spoke + mandatory fast-travel + Pokemon (seamless scroll INSIDE, hard-cut fade BETWEEN) + RuneScape chunk load-ahead (warm neighbors only).
AK design: replace the faux bgColor() tint-bands (one 2600x2600 world) with a ZONES registry of real discrete districts, each its own painted backdrop. 4 launch zones: HOME_TURF=the_lot (start, keeps ARENA so FIGHT NOW is 1 tap), DOWNTOWN=neon_night (shops/social), FACTORY_ROW=golden_industrial (production bldgs), THE_DOCKS=rain_docks (endgame). Locked districts 4-10 = silhouette + barrier (Collapsed Bridge/Police Checkpoint/Gang Blockade/Magical Ward) + countdown, never hidden. Micro = camera scroll (keep). Macro = hard-cut fade-to-#0A0A0A + gold Cinzel #dist title card. Fast-travel mandatory (radar band tap -> teleport via fade). World NEVER gates a fight. ZERO token payout this build.
PHASE-0 GATE (do FIRST): ONE edge transition HOME_TURF<->DOWNTOWN + radar fast-travel, prove the feel on Rich's real phone before scaling. No SW/worker-decode/View-Transitions yet.
Plan: (1) ZONES const inline {id,name,backdropUrl,worldW,worldH,tint,buildings,edges:{N/S/E/W:{to,spawn}},locked,unlockAt,barrierLabel}; activeZone=getZone(localStorage.ak_hub_zone||'HOME_TURF'); route draw()/loop() through activeZone. (2) distribute 12 buildings across zones (ARENA stays in start). (3) zoneCache + loadBackdrop via createImageBitmap(resizeWidth), never getImageData (CORS taint; ACAO:* on maps host). (4) draw: ONE drawImage of the camera window from the backdrop, else procedural-ground fallback. (5) edge gate EDGE=40px -> enterZone or showBarrier. (6) enterZone = in-page swap (fade, reposition >=1 tile in, loadBackdrop, warm neighbors, spawnGrace, persist). (7) radar fast-travel. (9) persist ak_hub_zone+ak_hub_pos (return from game.html -> Arena door). (11) bake glows into backdrop art. (12) deploy+verify.
Art: 4 device-width WebP hub backdrops (glows BAKED in) from the 4 painted cities; 3-4 barrier sprites; locked-district silhouettes; wire the 14 hub assets as facades/ground; gold title-card CSS.
Acceptance: real Android -> spawn neutral in HOME_TURF on painted backdrop, NO auto-enter; walk to edge -> gold fade -> land >=1 tile inside DOWNTOWN w/ gold title card, no bounce, >=~50fps; radar tap = teleport; FIGHT NOW from any zone -> battler; locked edge = barrier+countdown refuse; return from game.html -> Arena door; bounded memory (LRU); no canvas-taint; verified live.

## BUILD 2 -- TOWER-DEFENSE NIGHT RAIDS + MOVEMENT FIX
Pattern: Kingdom Rush barracks (rally-point auto-spawn defenders, melee-lock lane, timer respawn) + hero ability (6 commanders' tap-specials) + CoC low-rage (never lose cards on defense; loot = bonus not stolen) + flow-field horde pathing (Red Blob, 1 BFS field/target, O(1)/agent) + Last War day/night siege + Brawl Stars floating stick (recenters under thumb, magnitude=speed) + Pointer Events setPointerCapture (hand-rolled nipplejs). Scripted escalating waves (NOT adaptive).
AK design: NIGHT = "the Strays come out" -- strays spawn at edges, flow-field toward 1-2 production buildings; deployed deck auto-defends via engine.js; 6 commanders = the droppable hero ability; defensive buildings file K cards from a rally point (capped by Main Tower lvl). Broken buildings take REPAIRABLE damage per AK_RAID_DEFENSE_SYSTEM (1000HP/lvl, 3 primary + 2 secondary, never <L1, Generator cascade) w/ 3 damage-state overlays. Day/night off the hub tint clock; community horde scheduled ET evening, shown PT. MOVEMENT FIX (hub only, NOT game.html): floating stick gated to left ~45%, radial deadzone ~0.14, RESTORED analog magnitude=dog gait (trot/sprint -- biggest free win), frame-rate-independent smoothing, multi-touch guard. Enemies = strays/wild-animals/rival-turf, never zombie/war (positive-vibes law). Rewards bones/gold ONLY, never lose cards, zero ALK. HONEST: this is a feature build (engine has no siege/EventBus today), not just wiring.
Art: 3 stray types + 1 boss Alpha Stray; 3 damage overlays x5 production bldgs; defender + 6 commander FX (sprite-baked); 5 production facades (queued); joystick nub = gold crowned-B glyph sprite. Leonardo dead -> glyph fallbacks, don't gate.

## BUILD 3 -- WORLD-MAP + BOT BASES (snapshot-as-bot async raids)
Pattern: Boom Beach (bases APPEAR on your map, no queue) + snapshot-as-bot (Forza Drivatar $0 no-ML: freeze real loadouts as JSON, replay) + CoC trophy-band async + curated set-pieces + anti-bot-detection (full plausible profiles or the illusion collapses).
AK design: world-map on the existing Canvas2D hub (radar already has player dot/blips/tap/fade). Raidable BASE PINS appear near Home Turf, refreshed lazily on map-open. Tap pin -> preview -> game.html?raid=<snapshot_id> (mirror ?go=match). Snapshot-as-bot: serialize ak_profile on session-end; serve others' snapshots. 3-tier supply: A real snapshots, B cold-start synthetic (template+jitter from 48-card/4-faction/6-commander pool, NOT genetic PCG), C hand-curated story set-pieces. Build ak-bot-seed FIRST so the map is never empty. Matchmaking = M08 scout pool (ak-raid-find), tower-rank band. Reuse pure DamageCalculator (lootCap=anti-whale) + RaidController (CREW_UNDER_SIEGE rally + 24h revenge) + ShieldSystem inside Deno edge fns. Anti-detection: every bot a full plausible profile (handle/crew/WL/last-seen/faction/rank), never labeled, never the only opponents high-rank. Offline defense -> Defense Log + replay seed; surgical targeted dmg, 5-tier shields, auto 1h emergency shield, 24h revenge +25%, >14d -5%/day decay. CRYPTO (tightened): loot/shields/repairs/territory ALL soft currency; ALK cosmetic+geo-gated only. $BCARDD founder faceless in bot flavor.
Tables (mfghdobptredxxhbjwyz): base_snapshots, raid_log, defense_log, shields. Build order: ak-bot-seed -> ak-snapshot-upsert -> engine raid-mode adapter -> ak-raid-find pins -> ak-raid-resolve+log+seed -> shields/revenge -> crew reinforcement.
Art: 4 faction emblems; Lieutenant portraits (Enforcer/Dealer/Scout x Common->Legendary); 5 shield FX + 3 damage overlays (shared w/ B2); base-pin markers + preview-card chrome; procedural handles/crew-names/flavor (faceless). Glyph fallbacks until art backend funded.

## SOURCE
13-agent workflow wf_e1debb11-122 (areas: multimap/towerdef/worldraid/controls/techstack/social_crypto, all verified buildable). Full raw result archived in the run transcript.
